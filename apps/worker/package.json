{
  "name": "@omniretail/worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@omniretail/connector-noon": "workspace:*",
    "@omniretail/connector-salla": "workspace:*",
    "@omniretail/connector-sdk": "workspace:*",
    "@omniretail/connector-zid": "workspace:*",
    "@omniretail/db": "workspace:*",
    "bullmq": "^5.34.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
